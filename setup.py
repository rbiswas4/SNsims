from setuptools import setup, find_packages

setup(name="SNsims",
      version="0.0.1dev",
      description='SNsims produces SN simulations with Ia and core-collapse SN\
                based on `~sncosmo`',
      long_description=''' ''',
      packages=['snsims','tests', 'examples']
      )
